import React from "react";
// import  from "mjml-navbar"
import {} from "rrr";

export default function Navbar({ item }) {
  return (
    <section>
      <div>
        <nav>{item}</nav>
      </div>
    </section>
  );
}
